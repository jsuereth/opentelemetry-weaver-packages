version: '2'
attributes:
  - key: test.attr
    type: string
    brief: A test attribute
    stability: stable
metrics:
  - name: test.metric
    brief: A test metric
    stability: stable
    instrument: counter
    unit: "1"
    attributes:
      - ref: test.attr
        requirement_level: required
